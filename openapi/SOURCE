# Where this copy of the contract came from.
#
# The contract is authored in the private PhysionWebapps repo, which also runs
# the service it describes -- so that is the only place it can be kept true. This
# repo holds a copy because a public repo cannot submodule a private one without
# breaking every outside clone and CI run.
#
# A copy needs provenance or it becomes folklore. These lines say exactly which
# upstream revision this file is, so a human syncing it can see at a glance
# whether they are behind. scripts/check_contract.py verifies the hash still
# matches, which catches the copy being edited here instead of upstream -- the
# one change that would make the two silently disagree.
#
# The Node client (Physion-Labs/galileo-node) holds the same copy and the same
# provenance file. When the contract moves, both are synced, or the two SDKs
# start describing different APIs.

repo   = Physion-Labs/PhysionWebapps
commit = dd7f0849ac63b6910401e39baa30309c1641e8cb
path   = openapi/galileo-v1.yaml
sha256 = 0384b2e82d60d173b394002357c4d0d5327a66095a1f597a64fbb47a747a49f7
